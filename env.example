# Database Configuration
# In production, these should be set via System Environment Variables
DB_URL=jdbc:h2:mem:genesis;DB_CLOSE_DELAY=-1
DB_USERNAME=sa
DB_PASSWORD=password

# JWT Authentication
# In production, use a strong random secret of at least 256 bits
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-256-bits-minimum

# Server Port
PORT=8080
